# mmr_healthmap

This is a dcf data source project, initialized with `dcf::dcf_add_source`.

You can use the `dcf` package to check the project:

```R
dcf_check_source("mmr_healthmap", "..")
```

And process it:

```R
dcf_process("mmr_healthmap", "..")
```
